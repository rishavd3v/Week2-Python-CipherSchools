# tuple data structure
# tuplecan store any data type 
# most importatnt tuples are immutable, once tuples are created you can't  update data inside tuple

example = ('one', 'two', 'three')

# no append, no insert, no pop, no remove

days = ('monday', 'tuesday')

# tuples are faster than lists

# methods 
# count, index 
# len function 
# slicing 
example[0] = 1
print(example)